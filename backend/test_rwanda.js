const rwanda = require('rwanda');
console.log(Object.keys(rwanda));
// Let's see if there are arguments we can pass
console.log(rwanda.Sectors('Gasabo')); // Maybe?
console.log(rwanda.Sectors('Kigali City', 'Gasabo')); // Maybe?
