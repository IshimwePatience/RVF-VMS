const { LabResult } = require('./models'); async function removeDuplicates() { try { console.log('Starting duplicate cleanup...'); const results = await LabResult.findAll(); const grouped = {}; results.forEach(r => { if(r.sample_tracking_id) { grouped[r.sample_tracking_id] = grouped[r.sample_tracking_id] || []; grouped[r.sample_tracking_id].push(r); } }); let deletedCount = 0; for (const trackingId in grouped) { const records = grouped[trackingId]; if (records.length > 1) { records.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)); for (let i = 1; i < records.length; i++) { await records[i].destroy(); deletedCount++; } } } console.log('Successfully removed ' + deletedCount + ' duplicate lab results!'); process.exit(0); } catch (err) { console.error('Error:', err); process.exit(1); } } removeDuplicates();
